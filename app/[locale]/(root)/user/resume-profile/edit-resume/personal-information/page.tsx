import { UserFormRegister } from "@/components/form/user-form-register";

type EditePageProps = {};

const EditPageResume = () => {
	return (
		<main className="flex p-8">
			<UserFormRegister />
		</main>
	);
};

export default EditPageResume;
